{
  "name": "formeleven",
  "version": "0.0.1",
  "description": "",
  "keywords": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "npm run init && parcel build index.html 404.html terms.html",
    "clean-dist": "rm dist/*",
    "copy-files": "cp static/* dist",
    "dev": "parcel index.html 404.html terms.html index.js",
    "init": "npm run clean-dist && npm run copy-files",
    "start": "npm run init && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.3.2",
    "@parcel/transformer-elm": "^2.3.2",
    "@parcel/transformer-webmanifest": "^2.3.2",
    "parcel": "^2.0.1"
  },
  "dependencies": {
    "elm": "^0.19.1-5"
  }
}
